MOV FF,A;
	MOV A,OUT3;
	CALL CLOCK;
	MOV 11,A;
	MOV A,OUT3;
	CALL CLOCK;
	MOV 11,A;
	MOV A,OUT3;
	CALL CLOCK;
	MOV 11,A;
	MOV A,OUT3;
	CALL CLOCK;
	MOV FF,A;
	MOV A,OUT3;
	CALL CLOCK;

FIM:	JMP FIM;

CLOCK:
	MOV 00,A;
	MOV A,OUT2; //XXXXXXX0 XXXXXXX1 XXXXXX0
	MOV 01,A;
	MOV A,OUT2;
	MOV 00,A;
	MOV A,OUT2;
	RET;