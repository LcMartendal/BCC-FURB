COMPARACOES:
	MOV IN1,A;
	MOV A,B; //BACKUP DE IN1
	MOV IN2,A;

	SUB B,A;
	JMPZ IGUAL;
	//ELSE
	JMPC MAIOR;
	//ELSE
	JMP MENOR;
IGUAL:
	MOV 01,A;
	MOV A,OUT2;
	JMP COMPARACOES;

MAIOR:
	MOV 02,A;
	MOV A,OUT2;
	JMP COMPARACOES;

MENOR:
	MOV 03,A;
	MOV A,OUT2;
	JMP COMPARACOES;