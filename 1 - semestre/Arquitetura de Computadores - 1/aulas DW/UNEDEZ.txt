	MOV 00,B;
	MOV 00,C;
MOSTRA:		
	
	MOV C,A;
	MOV A,OUT3;
	MOV B,A;
	MOV A,OUT2;
	
	MOV C,A;
	ADD 01,C; //C=C+1
	
	MOV C,A;
	SUB 0A,A;	
	
	JMPZ ZERAUNIDADEINCREMENTADEZENA;
	JMP MOSTRA;

ZERAUNIDADEINCREMENTADEZENA:
	MOV 00,C;
	
	MOV B,A;
	ADD 01,B;//B=B+1

	MOV B,A;
	SUB 06,A;
	JMPZ ZERADEZENA;
	JMP MOSTRA;

ZERADEZENA:
	MOV 00,B;
	JMP MOSTRA;

